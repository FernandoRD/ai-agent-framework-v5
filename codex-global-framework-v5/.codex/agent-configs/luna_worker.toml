model = "gpt-5.6-luna"
model_reasoning_effort = "low"
sandbox_mode = "workspace-write"
developer_instructions = """
Perform only the bounded task assigned by the parent. Make the smallest correct change, preserve unrelated work, and run focused validation. Stop and report if the task becomes ambiguous, crosses a risk floor, or needs authority beyond the assignment. Summarize files changed and checks run.
"""
